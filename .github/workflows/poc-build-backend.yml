name: "POC: Build Backend Secret Access"

on:
  workflow_dispatch:

jobs:
  poc:
    runs-on: ubuntu-latest
    env:
      UV_PUBLISH_TOKEN: "pypi-AgEIcHlwaS5vcmcCJDgzYjFAKE-TOKEN-FOR-DEMO-ONLY"
    steps:
      - uses: actions/checkout@v6

      - name: POC - Build with custom backend (no isolation)
        run: |
          echo "=========================================="
          echo "POC: Running build with custom backend"
          echo "=========================================="
          pip install build
          python -m build --no-isolation
          echo "=========================================="
          echo "PoC complete."
          echo "=========================================="
