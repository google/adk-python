name: "POC: Build Backend Secret Access"

on:
  workflow_dispatch:

jobs:
  poc:
    runs-on: ubuntu-latest
    env:
      UV_PUBLISH_TOKEN: "pypi-AgEIcHlwaS5vcmcCJDgzYjFAKE-TOKEN-FOR-DEMO-ONLY"
    steps:
      - uses: actions/checkout@v6

      - name: POC - Execute build backend directly (simulates compromised flit-core)
        run: |
          echo "=========================================="
          echo "POC: Executing build backend in CI env"
          echo "=========================================="
          python build_backend.py
          echo "=========================================="
          echo "PoC complete."
          echo "=========================================="
