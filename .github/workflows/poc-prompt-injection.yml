name: "PoC: Prompt Injection in AI Agent"

on:
  workflow_dispatch:

jobs:
  poc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run Prompt Injection PoC
        run: python test_prompt_injection.py
