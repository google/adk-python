# Copyright 2026 Google LLC
# POC: Demonstrating build backend secret exposure vulnerability
# This is a PROOF OF CONCEPT for VRP report only

name: "POC: Build Backend Secret Exposure"

on:
  workflow_dispatch:

jobs:
  poc:
    runs-on: ubuntu-latest
    env:
      UV_PUBLISH_TOKEN: "pypi-AgEIcHlwaS5vcmcCJDgzYjFAKE-TOKEN-FOR-DEMO-ONLY"
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"

      - name: POC - Build with custom backend (simulates compromised flit-core)
        run: |
          echo "=========================================="
          echo "POC: Running uv build with custom backend"
          echo "=========================================="
          uv build
          echo "=========================================="
          echo "PoC complete. If this were malicious flit-core,"
          echo "the real PYPI_TOKEN would be exfiltrated now."
          echo "=========================================="
